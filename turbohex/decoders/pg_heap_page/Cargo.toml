[package]
name = "pg_heap_page"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
pg_arrow = { git = "https://github.com/pg-arrow/pg_arrow.git", branch = "main" }
